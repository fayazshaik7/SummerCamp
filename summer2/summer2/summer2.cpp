// summer2.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
/*#include <stdio.h>
#include <stdlib.h> 
#include <iostream>
using namespace std;
long fib(int n,int *arr){
	if (arr[n] > 0)
		return arr[n];
	if (n == 0 || n == 1){
		arr[n] = n;
		return arr[n];
	}
	arr[n] = fib(n - 1,arr) + fib(n - 2,arr);
	return arr[n];

}

long find_sum(int arr[], int n){
	if (n <= 0)
		return 0;
	return arr[n - 1] + find_sum(arr, n - 1);
}/*
int _tmain(int argc, _TCHAR* argv[])
{
	/*int arr[5] = { 1, 2, 3, 4, 5 };
	printf("sum = %ld\n", find_sum(arr, 5));
	return 0;
	int *arr;
	int n;
	cin >> n;
	arr = (int *)malloc(sizeof(int) * n);
	for (int i = 0; i < n; i++)
		arr[i] = 0;
	printf("%d \n", fib(-1,arr));
}
*/
